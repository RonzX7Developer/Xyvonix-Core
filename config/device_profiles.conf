# Xyvonix Core v3.0 runtime policy
# Adaptive detection is preferred over fixed Redmi/SoC node paths.
# Target: AxManager non-root, Xiaomi/Redmi/POCO and Android 14–16.
DEFAULT_PROFILE=balanced
MAX_SAFE_PROFILE=performance
THERMAL_BYPASS=false
SELINUX_BYPASS=false
ROOT_REQUIRED=false
